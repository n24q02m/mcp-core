services:
  embedding-daemon:
    image: n24q02m/mcp-embedding-daemon:latest
    ports:
      - "127.0.0.1:8001:8001"
    volumes:
      - embedding-cache:/cache
    restart: unless-stopped

volumes:
  embedding-cache:
